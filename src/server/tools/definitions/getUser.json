{
  "name": "getUser",
  "description": "Gets details about a specific user. Corresponds to GET /api/users/v1/users/{userId}. Requires `users:read` scope. [READ-ONLY]",
  "inputSchema": {
    "type": "object",
    "properties": {
      "userId": {
        "type": "string",
        "description": "The ID of the user to retrieve"
      }
    },
    "required": [
      "userId"
    ]
  },
  "category": "read-only",
  "type": "user",
  "dangerous": false,
  "httpType": "GET",
  "url": "/api/users/v1/users/:userId"
}
