{
  "name": "removeInterfaceStationAssignment",
  "description": "⚠️ ADMIN OPERATION ⚠️ Removes station assignment from an interface. Corresponds to DELETE /api/stations/v1/interfaces/{interfaceId}/station-assignment. Requires `stations:write` scope. [ADMIN]",
  "inputSchema": {
    "type": "object",
    "properties": {
      "interfaceId": {
        "type": "string",
        "description": "The ID of the interface to remove station assignment from"
      }
    },
    "required": [
      "interfaceId"
    ]
  },
  "category": "admin",
  "type": "interface",
  "dangerous": true,
  "httpType": "DELETE",
  "url": "/api/stations/v1/interfaces/:interfaceId/station-assignment"
}
